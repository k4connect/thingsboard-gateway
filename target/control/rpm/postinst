#!/bin/sh

chown -R thingsboard: /var/log/tb-gateway
chown -R thingsboard: /usr/share/tb-gateway

if [ $1 -eq 1 ] ; then
        # Initial installation
        systemctl --no-reload enable tb-gateway.service >/dev/null 2>&1 || :
fi
